import { SanitizationKernel } from './artifact_sanitization_kernel'; const kernel = SanitizationKernel; kernel.execute_recursive_layer_sanitization(); (async () => { const digest_v1 = 'SHA256-CORE-IMG-001'; const s1 = await kernel.attest_artifact('coregraph_main', digest_v1); const vitality = kernel.get_integrity_vitality(); if (vitality.integrity_attestation === 1.0 && vitality.attested === 1 && s1) { console.log('[+] SANITIZATION CORE: VALIDATION PASSED (F_int = 1.0)'); process.exit(0); } else { process.exit(1); } })();
