import { PickingKernel } from './ocular_picking_kernel'; const kernel = PickingKernel; const mock_matrix = new Float32Array(16); kernel.update_inverse_vp_matrix(mock_matrix); const ray = kernel.execute_matrix_unprojection_sequence(400, 300, 800, 600); const vitality = kernel.get_ocular_vitality(); if (vitality.ocular_integrity === 1.0 && ray) { console.log('[+] OCULAR CORE: VALIDATION PASSED (F_ocu = 1.0)'); process.exit(0); } else { process.exit(1); }
