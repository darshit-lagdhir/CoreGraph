import { ScalingKernel } from './centrality_scaling_kernel'; const kernel = ScalingKernel; const ranks = new Float32Array(100000).map(() => Math.random() * 100); const mock_gl = {} as any; kernel.execute_logarithmic_radius_mapping(mock_gl, ranks); const vitality = kernel.get_hierarchy_vitality(); if (vitality.hierarchy_integrity === 1.0 && vitality.nodes_scaled === 100000) { console.log('[+] SCALING CORE: VALIDATION PASSED (F_hie = 1.0)'); process.exit(0); } else { process.exit(1); }
