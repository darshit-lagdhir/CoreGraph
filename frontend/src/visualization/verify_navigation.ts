import { CameraKernel } from './camera_alignment_kernel'; const kernel = CameraKernel; const camera = { position: [0,0,10], target: [0,0,0], fov: 45, aspect: 1, near: 0.1, far: 1000 }; const matrix = kernel.execute_perspective_transformation_sequence(camera); const vitality = kernel.get_navigation_vitality(); if (vitality.navigation_integrity === 1.0 && matrix) { console.log('[+] NAVIGATION CORE: VALIDATION PASSED (F_nav = 1.0)'); process.exit(0); } else { process.exit(1); }
