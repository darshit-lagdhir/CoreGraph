import { ReadoutKernel } from './readout_binding_kernel'; const kernel = ReadoutKernel; kernel.execute_readout_panel_initialization(); kernel.select_node('node-1', { id: 'node-1', name: 'react', version: '18.2.0', cvi_score: 95, maintainer_count: 15, funding_status: -500, last_inspected: 0 }); const vitality = kernel.get_inspection_vitality(); const state = kernel.get_hydrated_readout(); if (vitality.inspection_integrity === 1.0 && state?.name === 'react') { console.log('[+] READOUT CORE: VALIDATION PASSED (F_ins = 1.0)'); process.exit(0); } else { process.exit(1); }
