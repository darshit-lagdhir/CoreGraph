import { FormattingKernel, EFormattingType } from './metric_formatting_kernel'; const kernel = FormattingKernel; kernel.execute_high_fidelity_metric_initialization(); const usd = kernel.format(1250.50, EFormattingType.CURRENCY); const compact = kernel.format(1000000, EFormattingType.COMPACT); const vitality = kernel.get_fiscal_vitality(); if (vitality.fiscal_integrity === 1.0 && usd === ',250.50' && compact === '1M') { console.log('[+] FORMATTING CORE: VALIDATION PASSED (F_fis = 1.0)'); process.exit(0); } else { console.log('USD:', usd, 'Compact:', compact); process.exit(1); }
