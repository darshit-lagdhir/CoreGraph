import { SanitationKernel } from './sanitation_kernel'; const kernel = SanitationKernel; kernel.execute_aggressive_asset_eradication(); (async () => { const s1 = await kernel.verify_structural_purity('e2e-tests/temp_trace.ts'); const vitality = kernel.get_purity_vitality(); if (vitality.production_integrity === 1.0 && vitality.eradicated === 1 && s1) { console.log('[+] SANITATION CORE: Purity SEALED (F_arc = 1.0)'); process.exit(0); } else { process.exit(1); } })();
